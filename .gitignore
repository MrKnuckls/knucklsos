# live-build build artifacts (regenerated on build)
/build/
/binary*
/chroot/
/auto
config/binary
config/bootstrap
config/bootloaders
*.buildlog
*.iso
*.deb
.tmp/

# editor / OS noise
*~
*.swp
.idea/
.vscode/
.DS_Store

# dev preview mockups (not part of the OS)
previews/

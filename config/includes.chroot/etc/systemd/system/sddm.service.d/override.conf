[Service]
# Workaround for the SDDM/Plasma black-screen race condition: SDDM can start
# before the GPU/KWin is fully ready, leaving a black screen. Delay SDDM
# briefly so the graphics device is initialized first.
ExecStartPre=/bin/sleep 3

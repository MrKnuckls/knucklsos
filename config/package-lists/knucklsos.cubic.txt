# Plain package list for Cubic (Path A in README).
# Paste these (space separated) into Cubic's terminal:
#   sudo apt install -y <all these>
linux-generic kde-plasma-desktop sddm plasma-nm plasma-pa dolphin konsole kdeconnect breeze breeze-gtk-theme papirus-icon-theme fonts-noto-core fonts-liberation steam lutris wine wine64 wine32:i386 gamemode mangohud vulkan-tools mesa-vulkan-drivers mesa-vulkan-drivers:i386 mesa-utils libvulkan1:i386 libgl1:i386 libglx-mesa0:i386 xpad xboxdrv nvidia-driver-550 flatpak gnome-software-plugin-flatpak unzip curl wget ca-certificates
# Then run the Chrome hook manually:
#   wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
#   sudo apt install -y ./google-chrome-stable_current_amd64.deb

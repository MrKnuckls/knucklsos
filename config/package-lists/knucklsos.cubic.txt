# Plain package list for manual builds (Cubic / apt). Debian equivalents.
# Paste these (space separated) into a terminal:
#   sudo apt update && sudo apt install -y <all these>
linux-image-amd64 kde-plasma-desktop sddm plasma-nm plasma-pa dolphin konsole kdeconnect breeze breeze-gtk-theme papirus-icon-theme fonts-noto-core fonts-liberation steam lutris wine wine64 wine32:i386 gamemode mangohud vulkan-tools mesa-vulkan-drivers mesa-vulkan-drivers:i386 mesa-utils libvulkan1:i386 libgl1:i386 libglx-mesa0:i386 intel-microcode amd64-microcode libgl1-mesa-dri mesa-va-drivers libvulkan1 nvidia-driver nvidia-detect calamares calamares-settings-debian pciutils polkit-kde-agent-1 plymouth-themes flatpak gnome-software-plugin-flatpak unzip curl wget ca-certificates
# Then run the Chrome hook manually:
#   wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
#   sudo apt install -y ./google-chrome-stable_current_amd64.deb
